import styled from "styled-components";

export const Container = styled.div`
	padding: 1em 0 2em;
	display: flex;
	flex-wrap: wrap;
	gap: 1em;
	& img {
		width: 180px;
		height: 180px;
		object-fit: cover;
	}
`
